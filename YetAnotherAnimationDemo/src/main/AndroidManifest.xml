<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sergedesmedt.yaad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".YetAnotherAnimationDemoActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SimpleViewAnimationActivity" />
        <activity
            android:name=".SimplePropertyAnimationActivity"
            android:label="@string/title_activity_simple_property_animation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SimpleLayoutAnimationActivity"
            android:label="@string/title_activity_simple_layout_animation"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
